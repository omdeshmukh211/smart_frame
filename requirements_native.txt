# Requirements for Smart Frame - Native PyQt5 Version
# Production-ready dependencies for Raspberry Pi

# YAML configuration support
PyYAML>=5.4.0

# YouTube Music Integration (headless)
yt-dlp>=2023.3.4

python-mpv

# Note: mpv must be installed via system package manager
# On Raspberry Pi/Debian: sudo apt-get install mpv
# On macOS: brew install mpv
# On Windows: Download from https://mpv.io/

# Optional: Image processing (if needed for advanced photo features)
# Pillow>=9.0.0
