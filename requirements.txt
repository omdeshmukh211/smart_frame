# Requirements for Smart Frame - System-wide Installation
# All dependencies installed system-wide on Raspberry Pi

# Step 1: Install system packages via apt:
#   sudo apt update
#   sudo apt install -y python3-pyqt5 python3-pyqt5.qtmultimedia python3-pyqt5.qtcore
#   sudo apt install -y python3-pyqt5.qtgui python3-pyqt5.qtwidgets python3-pyqt5.qtnetwork
#   sudo apt install -y mpv ffmpeg python3-pip python3-yaml

# Step 2: Install Python packages system-wide via pip:
#   sudo pip3 install yt-dlp>=2023.3.4 python-mpv

# Python packages (install with: sudo pip3 install -r requirements.txt):
PyYAML>=5.4.0
yt-dlp>=2023.3.4
python-mpv

# Optional: Image processing (if needed for advanced photo features)
# Pillow>=9.0.0
